# Handoff Note — 12 March 2026 (morning pickup)

## What We Were Doing
Polishing the Subfracture v2 SvelteKit site to match/exceed the live v1 WordPress site (subfrac.com), then preparing GEPA optimization scripts.

## What's DONE

### Assets Downloaded
- **Team photos** → `static/images/team/` (warwick, casey, tyronne, amanda .png)
- **Client logos** → `static/images/clients/` (nike.png, sprite.svg, bmw.svg, sennheiser.svg, jimmy-choo.svg, qld-government.svg, senn-2.svg, verizon.svg)
- **SF logo** → `static/subfracture.svg`

### Component Updates Landed
1. **TeamSection.svelte** — Roster fixed (Casey Midgley replaces Maddi Kimbe). Full team data: photos, bios, LinkedIn URLs for all 4 members.
2. **TeamMember.svelte** — Expanded to interactive click-to-expand card: photo, bio panel, LinkedIn link, `aria-expanded`, focus-visible, reduced motion support.
3. **ClientsReveal.svelte** — ✅ JUST COMPLETED THIS SESSION: Swapped text `<span>` placeholders for `<img>` tags using downloaded logo files. Added `.logo-img` styles (grayscale filter, hover reveal to full color). Data array now has `src` paths.

## What's NOT Done (pick up here)

### Pre-GEPA Content Parity (2 remaining)

| Task | Status | Details |
|---|---|---|
| **pre-6: Hero frosted glass overlay** | NOT DONE | Agent was dispatched (`ses_323075a66ffeu66oOEVK15dLbh`) but didn't complete. Need to add frosted glass overlay to `VideoHero.svelte` — blur(60px) + grayscale + white overlay default, hover reveals full color + custom "▶ Sizzle Me" cursor pill. v1 CSS pattern documented in handoff context. |
| **pre-7: IntroBlock component** | NOT DONE | Agent was dispatched (`ses_3230704daffeQfVQ6ZigLukOOn`) but file was never created. Need new `src/lib/components/content/IntroBlock.svelte` with H1 "Strategic Creative Design Technology", lede paragraph, animated purple orb glow. Then wire into `+page.svelte` between `<Hero />` and `<ClientsReveal />`. |

### GEPA Scripts (3 total, none written yet)

| Script | Target | Notes |
|---|---|---|
| `optimize_site_v2.py` | `tokens.css` (88 lines) | Updated version of existing `optimize_site.py`. Must reflect current section order: Hero → IntroBlock → ClientsReveal → ProblemsSolved → Services → ClosingStatement → InlineCTA → Portfolio → Team → Contact. 8 scroll-position screenshots, VLM-scored via CLIProxyAPI. |
| `optimize_hero_experience.py` | `VideoHero.svelte` CSS | Focused on hero overlay timing, blur amount, cursor behaviour, watermark parallax. Single-section screenshots only. |
| `optimize_team_footer.py` | Team + Footer CSS | Team card layout, bio expansion, footer spacing. 2-3 section screenshots. |

### Build Verification
- **Not yet run** — the ClientsReveal edit needs `npm run build` verification
- TeamSection/TeamMember edits from prior session also unverified

## Key File Locations
- `src/lib/components/hero/VideoHero.svelte` — needs frosted glass (pre-6)
- `src/lib/components/content/IntroBlock.svelte` — needs creating (pre-7)
- `src/routes/+page.svelte` — needs IntroBlock import added
- `src/lib/components/hero/ClientsReveal.svelte` — ✅ logos done
- `src/lib/styles/tokens.css` — GEPA mutation target (88 lines)
- `optimize_site.py` — existing GEPA reference (402 lines)
- `optimize_hero.py` — existing GEPA reference (409 lines)

## Existing Agent Sessions (can resume with session_id)
- Hero frosted glass: `ses_323075a66ffeu66oOEVK15dLbh`
- IntroBlock: `ses_3230704daffeQfVQ6ZigLukOOn`

## Recommended Resume Order
1. `npm run build` — verify current state compiles
2. pre-6: Hero frosted glass overlay (VideoHero.svelte)
3. pre-7: IntroBlock component (new file + page wiring)
4. `npm run build` — verify again
5. gepa-1: Write `optimize_site_v2.py`
6. gepa-2: Write `optimize_hero_experience.py`  
7. gepa-3: Write `optimize_team_footer.py`
8. Run GEPA (requires dev server + CLIProxyAPI)

## Creative Constraints (don't forget)
- "Restraint = authority. White space is the primary material."
- No new dependencies — GSAP + Lenis + Tailwind only
- No refactoring while fixing
- Respect `prefers-reduced-motion` everywhere
- Build must pass after every change
