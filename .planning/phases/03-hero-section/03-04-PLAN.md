---
phase: 03-hero-section
plan: 04
type: execute
wave: 3
depends_on: ["03-03"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Visitor understands Subfracture is a Culture Studio within 5 seconds of page load"
    - "Botanical illustration is visible and establishes editorial/medical aesthetic"
    - "Hero content is readable and illustration adapts appropriately on mobile viewport"
    - "Users with prefers-reduced-motion see static version without animation"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete hero section visually and functionally across desktop, mobile, and reduced motion states.

Purpose: The hero is the first impression — it must be verified by a human for visual quality, animation timing, and mobile responsiveness before marking Phase 3 complete.

Output: Verification confirmation or list of issues to address.
</objective>

<execution_context>
@/home/dom/.claude/get-shit-done/workflows/execute-plan.md
@/home/dom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-hero-section/03-CONTEXT.md
@.planning/phases/03-hero-section/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and verify build</name>
  <files></files>
  <action>
Run `npm run build` to verify the production build succeeds. Then start the dev server with `npm run dev` and capture the local URL.

Check for:
- No TypeScript compilation errors
- No SSR errors in build output
- Dev server starts successfully
  </action>
  <verify>
`npm run build` exits with 0. `npm run dev` starts and provides a localhost URL.
  </verify>
  <done>Build succeeds and dev server is running.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete hero section with:
1. Full-viewport hero (100svh) with abstract organic SVG illustration as background
2. "Culture Studio" headline, tagline, subline, and two CTAs overlaying the illustration
3. GSAP DrawSVG entrance animation: SVG paths draw in first, then text reveals, carousel fades last (~1s total)
4. Basic parallax on scroll (illustration shifts slower than text)
5. Logo carousel with placeholder brand marks at bottom edge
6. Mobile treatment: fewer SVG paths, stacked CTA buttons
7. Reduced motion: all elements visible immediately, no animation
  </what-built>
  <how-to-verify>
**Desktop verification (open dev server in browser at full width):**
1. Page loads — do you see the SVG line-drawing animation play first, followed by text revealing on top?
2. Does the hero fill the full viewport height?
3. Can you read "Culture Studio" clearly against the illustration background?
4. Are the CTAs ("Start a conversation" / "See the work") understated text links, NOT buttons?
5. Do the CTAs link to #contact and #work sections respectively?
6. Is the logo carousel visible at the bottom edge with placeholder marks scrolling?
7. Scroll down — does the illustration have a subtle parallax shift (moves slower than content)?
8. Does the illustration feel like "medical textbook meets art book" — scholarly, organic, warm?

**Mobile verification (resize browser to ~375px width or use DevTools mobile):**
9. Is the SVG illustration simplified (fewer visible paths)?
10. Are CTAs full-width stacked buttons?
11. Does the hero still fill the viewport height?
12. Is text readable on mobile?

**Reduced motion verification (enable prefers-reduced-motion in DevTools → Rendering):**
13. Toggle `prefers-reduced-motion: reduce` in browser DevTools (Rendering panel)
14. Reload — are ALL elements visible immediately with zero animation?
15. Is the logo carousel static (not scrolling)?

**Console check:**
16. Open browser DevTools Console — are there any errors or hydration warnings?
  </how-to-verify>
  <resume-signal>Type "approved" if the hero looks and works correctly, or describe any issues that need fixing.</resume-signal>
</task>

</tasks>

<verification>
All four success criteria from ROADMAP.md Phase 3 are verified:
1. Visitor understands Subfracture is a Culture Studio within 5 seconds of page load
2. Botanical illustration is visible and establishes editorial/medical aesthetic
3. Hero content is readable and illustration adapts appropriately on mobile viewport
4. Users with prefers-reduced-motion see static version without animation
</verification>

<success_criteria>
- Human confirms hero visual quality, animation timing, mobile behavior, and reduced motion
- No console errors or hydration warnings
- All four Phase 3 success criteria met
</success_criteria>

<output>
After completion, create `.planning/phases/03-hero-section/03-04-SUMMARY.md`
</output>
