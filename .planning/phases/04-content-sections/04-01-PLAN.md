---
phase: 04-content-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/components/content/ProblemCard.svelte
  - src/lib/components/content/ProblemsSolved.svelte
  - src/lib/components/content/ServicePillar.svelte
  - src/lib/components/content/Services.svelte
  - src/lib/components/content/ClosingStatement.svelte
autonomous: true

must_haves:
  truths:
    - "ProblemsSolved component renders section heading, intro paragraph, 'Problems we help solve' label, and 6 confrontational pain point cards"
    - "Services component renders 'What we build' label and 5 numbered service pillars (01-05) each with tagline, description, and bullet list"
    - "ClosingStatement component renders the key closing line with visual weight and closing paragraph"
    - "All content matches CONTENT.md exactly — no copywriting, no paraphrasing"
    - "Problem cards use confrontational typographic treatment — borderless, large serif, no soft backgrounds"
    - "Scroll-triggered staggered reveals animate cards and pillars into view"
    - "Reduced motion users see all content without animation"
  artifacts:
    - path: "src/lib/components/content/ProblemCard.svelte"
      provides: "Single pain point card with confrontational typography"
      min_lines: 15
    - path: "src/lib/components/content/ProblemsSolved.svelte"
      provides: "Section heading, intro, label, 6 problem cards with staggered scroll reveal"
      min_lines: 80
    - path: "src/lib/components/content/ServicePillar.svelte"
      provides: "Single service with number, tagline, description, bullet list"
      min_lines: 40
    - path: "src/lib/components/content/Services.svelte"
      provides: "Section label, 5 service pillars with staggered scroll reveal"
      min_lines: 100
    - path: "src/lib/components/content/ClosingStatement.svelte"
      provides: "Key closing line and paragraph with scroll reveal"
      min_lines: 40
  key_links:
    - from: "src/lib/components/content/ProblemsSolved.svelte"
      to: "src/lib/components/content/ProblemCard.svelte"
      via: "import and {#each} render"
      pattern: "import ProblemCard"
    - from: "src/lib/components/content/Services.svelte"
      to: "src/lib/components/content/ServicePillar.svelte"
      via: "import and {#each} render"
      pattern: "import ServicePillar"
    - from: "src/lib/components/content/ProblemsSolved.svelte"
      to: "src/lib/components/ui/Container.svelte"
      via: "layout wrapper"
      pattern: "import Container"
    - from: "src/lib/components/content/Services.svelte"
      to: "src/lib/components/ui/Container.svelte"
      via: "layout wrapper"
      pattern: "import Container"
---

<objective>
Build all five content section components: ProblemsSolved (with ProblemCard), Services (with ServicePillar), and ClosingStatement. Each component is self-contained with scroll-triggered GSAP reveals, uses existing Container and SectionHeading primitives, and renders content exactly from CONTENT.md.

Purpose: Create the editorial content layer between hero and portfolio that communicates what Subfracture does, what problems it solves, and how it works — with confrontational tone for problems, editorial numbered stack for services, and weighted closing statement.

Output: 5 new Svelte components in `src/lib/components/content/` ready for page integration.
</objective>

<execution_context>
@/home/dom/.claude/get-shit-done/workflows/execute-plan.md
@/home/dom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/CONTENT.md
@.planning/phases/04-content-sections/04-CONTEXT.md
@.planning/phases/04-content-sections/04-RESEARCH.md
@src/lib/components/ui/Container.svelte
@src/lib/components/ui/SectionHeading.svelte
@src/lib/actions/animate.ts
@src/lib/styles/tokens.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ProblemCard and ProblemsSolved components</name>
  <files>
    src/lib/components/content/ProblemCard.svelte
    src/lib/components/content/ProblemsSolved.svelte
  </files>
  <action>
Create `src/lib/components/content/` directory if it doesn't exist.

**ProblemCard.svelte:**
- Props: `{ text: string }` using `$props()`
- Renders a single pain point as borderless typographic card
- Uses Playfair Display at `--text-xl` (30px), weight 400, color `--color-text`
- NO background, NO border, NO shadow — stark confrontational treatment
- CSS class `.problem-card` for GSAP stagger targeting
- Initial state: `opacity: 0; transform: translateY(24px)` in CSS to prevent flash (Pitfall #2 from research)
- Generous bottom margin (2rem) for breathing room between cards

**ProblemsSolved.svelte:**
- Import Container, SectionHeading, ProblemCard, and animate action
- Section ID: `id="about"` (matches existing nav link `#about`)
- Background: `--color-surface` (default cream)
- Padding: `--spacing-section` top/bottom (200-300px per user decision on generous spacing — use `clamp(12rem, 15vw, 18rem)` for generous vertical gaps)

Content structure (exact copy from CONTENT.md):
1. SectionHeading with number="01" and title="Where art and systems flow together" — wrapped in `use:animate` div (opacity 0->1, y 24->0, 0.6s, power3.out, scrollTrigger start 'top 85%')
2. Intro paragraph: "Subfracture is a strategic culture and design studio based in Merivale Studios, South Brisbane. We help brands and IP get clear, get chosen, and get remembered — not just launched." — Source Sans 3, `--text-lg`, `--color-text-muted`, max-width 55ch. Animate with 0.15s delay after heading.
3. Subsection label: "Problems we help solve" — Source Sans 3, `--text-xs`, weight 600, `--color-primary`, uppercase, letter-spacing 0.1em. Margin-top 4rem.
4. Problem grid: 2 columns on desktop (grid-template-columns: 1fr 1fr), 1 column on mobile (below 48rem). Contains 6 ProblemCard components rendered via {#each}.

Staggered card animation: Use `onMount` + `gsap.context()` pattern from research:
- Dynamic import gsap and ScrollTrigger inside onMount (SSR safety)
- Use `gsap.matchMedia()` with two branches:
  - `(prefers-reduced-motion: no-preference)`: `gsap.from('.problem-card', { opacity: 0, y: 24, duration: 0.5, stagger: 0.08, ease: 'power3.out', scrollTrigger: { trigger: gridEl, start: 'top 80%' } })`
  - `(prefers-reduced-motion: reduce)`: `gsap.set('.problem-card', { opacity: 1, y: 0 })`
- Scope context to grid container element via `bind:this`
- Clean up with `ctx?.revert()` in `onDestroy`

Stats placeholder: Add an empty `<div class="stats-placeholder" aria-hidden="true">` after the problem grid with a comment noting CONT-04 insertion point.

The 6 problems (exact text from CONTENT.md):
1. "Campaign-led thinking with no long-term platform"
2. "Inconsistent brand and IP expression at scale"
3. "Low trust or weak cultural relevance"
4. "Launches that need real momentum, not noise"
5. "Brand systems that extend beyond start and end dates"
6. "Unclear positioning — people don't get it"
  </action>
  <verify>
- `ls src/lib/components/content/ProblemCard.svelte src/lib/components/content/ProblemsSolved.svelte` shows both files exist
- `npx svelte-check --threshold warning` passes without errors related to these files
- ProblemCard has `.problem-card` class with `opacity: 0` initial CSS state
- ProblemsSolved has `id="about"`, imports Container and SectionHeading, contains all 6 problem texts from CONTENT.md
- onDestroy cleanup exists (`ctx?.revert()`)
  </verify>
  <done>
ProblemsSolved component renders section heading ("Where art and systems flow together"), intro paragraph, "Problems we help solve" label, and 6 borderless typographic problem cards in a 2-column grid (1-column mobile). Cards stagger in on scroll with 0.08s between each. Reduced motion shows all content instantly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ServicePillar, Services, and ClosingStatement components</name>
  <files>
    src/lib/components/content/ServicePillar.svelte
    src/lib/components/content/Services.svelte
    src/lib/components/content/ClosingStatement.svelte
  </files>
  <action>
**ServicePillar.svelte:**
- Props: `{ number: string; tagline: string; description: string; bullets: string[] }` using `$props()`
- Layout: Vertical numbered stack (NOT cards, NOT accordion — per research recommendation)
- Structure:
  - Service number: Source Sans 3, `--text-xs`, weight 500, `--color-primary`, letter-spacing 0.05em
  - Service tagline: Playfair Display, `--text-2xl` (39px), weight 400, `--color-text`, margin-top 0.5rem
  - Service description: Source Sans 3, `--text-base` (18px), weight 400, `--color-text-muted`, max-width 55ch, margin-top 1rem
  - Bullet list: Source Sans 3, `--text-sm` (14px), `--color-text-muted`, list-style none, custom terracotta dash markers or small dots
- CSS class `.service-pillar` for GSAP stagger targeting
- Initial state: `opacity: 0; transform: translateY(24px)` in CSS (flash prevention)
- Thin divider between pillars: 1px border-bottom `--color-bone` (applied to all but last — use `:last-child` to remove)
- Generous padding-bottom (3rem) between services for breathing room
- Mobile: Single column, full width — no layout change needed since it's already vertical stack

**Services.svelte:**
- Import Container, ServicePillar, and animate action
- Section ID: `id="services"` (matches existing nav link `#services`)
- Background: `--color-surface-warm` (warm cream — background shift from Problems section)
- Padding: generous vertical gaps matching ProblemsSolved (use same clamp pattern)

Content structure:
1. Section label: "What we build" — Source Sans 3, `--text-xs`, weight 600, `--color-primary`, uppercase, letter-spacing 0.1em. Wrapped in `use:animate` for scroll reveal (opacity 0->1, y 24->0, 0.6s, power3.out, scrollTrigger start 'top 85%').
2. Services container: div with `bind:this={servicesEl}` containing 5 ServicePillar components rendered via {#each} from a data array.

Service data array (exact content from CONTENT.md):
```
01 — Strategy: "The foundation everything else sits on."
Description: "We define what the world stands for, where it competes, and why it matters — so decisions stop fragmenting as pressure increases."
Bullets: Positioning and value proposition | Audience and category tension mapping | Brand architecture (products, sub-brands, offers) | Experience strategy across touchpoints

02 — Creative Platforms: "A system you can build momentum on."
Description: "We create platform ideas that hold together across campaigns, channels, and moments — without resetting every quarter."
Bullets: Central creative platform and territories | Campaign system across channels | Content pillars and repeatable formats | Launch narrative and key moments

03 — Brand & Visual Identity: "Make it unmistakable."
Description: "We design identities that scale, adapt, and stay coherent wherever the brand shows up."
Bullets: Naming support and brand story | Tone of voice and verbal identity | Visual identity (logo, type, colour, layout, art direction) | Guidelines and templates for teams and partners

04 — Design & Build: "Bring the system to life."
Description: "We design and build the things people actually interact with — quickly, carefully, and with intent."
Bullets: Websites and landing pages (design and build) | Content production (design, motion, photography direction) | Prototyping before major investment | Software, dev, and immersive builds (where relevant)

05 — Partnerships & Cultural Influence: "Extend reach. Build trust."
Description: "We help brands create meaningful moments and collaborations that earn relevance, not just attention."
Bullets: Partnership strategy and collaborator shortlists | Talent and creator liaison | Cultural moments and PR-style thinking | Rollout planning to sustain momentum
```

Staggered pillar animation: Same `onMount` + `gsap.context()` + `gsap.matchMedia()` pattern as ProblemsSolved:
- `(prefers-reduced-motion: no-preference)`: `gsap.from('.service-pillar', { opacity: 0, y: 24, duration: 0.6, stagger: 0.12, ease: 'power3.out', scrollTrigger: { trigger: servicesEl, start: 'top 80%' } })`
- `(prefers-reduced-motion: reduce)`: `gsap.set('.service-pillar', { opacity: 1, y: 0 })`
- Clean up with `ctx?.revert()` in `onDestroy`

**ClosingStatement.svelte:**
- Import Container and animate action
- Background: `--color-surface` (back to primary cream — background shift from Services)
- Padding: generous vertical gaps, slightly less than full sections (use `clamp(8rem, 10vw, 12rem)`) since this is a bridge element, not a full section
- NO section ID (not a nav target)

Content structure:
1. Closing statement line: "Entry points are flexible. Outputs are deliberate." — Playfair Display, `--text-2xl` (39px), weight 400, `--color-text`. Give this visual weight — centered alignment, generous margin. Wrapped in `use:animate` (opacity 0->1, y 24->0, 0.7s, power3.out, scrollTrigger start 'top 80%').
2. Closing paragraph: "We work alongside founders and brand teams to set direction, design systems, and bring ideas to life — because strong brands aren't just seen, they're understood, trusted, and chosen." — Source Sans 3, `--text-base`, `--color-text-muted`, max-width 55ch, centered. Wrapped in `use:animate` (opacity 0->1, y 16->0, 0.5s, 0.2s delay, power3.out, scrollTrigger start 'top 80%').
3. Center both elements: text-align center, margin-inline auto.
  </action>
  <verify>
- `ls src/lib/components/content/ServicePillar.svelte src/lib/components/content/Services.svelte src/lib/components/content/ClosingStatement.svelte` shows all 3 files exist
- `npx svelte-check --threshold warning` passes without errors
- Services.svelte contains all 5 service entries with correct numbers (01-05), taglines, descriptions, and bullets matching CONTENT.md
- ClosingStatement contains exact closing text from CONTENT.md
- ServicePillar has `.service-pillar` class with `opacity: 0` initial CSS state
- All components have onDestroy cleanup where imperative GSAP is used
  </verify>
  <done>
Services component renders "What we build" label and 5 numbered service pillars (01-05) in vertical stack layout with thin dividers. Each pillar shows number, tagline, description, and 4 bullets. Pillars stagger in on scroll with 0.12s between each. ClosingStatement renders the weighted closing line and paragraph, centered. Reduced motion shows all content instantly.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. All 5 component files exist in `src/lib/components/content/`
2. `npx svelte-check --threshold warning` reports no errors
3. All content matches `.planning/CONTENT.md` exactly
4. Each component with imperative GSAP has `onDestroy` cleanup
5. CSS initial states set `opacity: 0` on animated elements (flash prevention)
6. ProblemsSolved section ID is `about`, Services section ID is `services`
7. Background alternation: Problems = `--color-surface`, Services = `--color-surface-warm`, Closing = `--color-surface`
</verification>

<success_criteria>
1. Five content components built with correct typography hierarchy, colors, and spacing from design system tokens
2. All copy matches CONTENT.md exactly — no paraphrasing or copywriting
3. GSAP scroll-triggered animations with staggered reveals for cards/pillars
4. Reduced motion handled via gsap.matchMedia() branches
5. Proper GSAP cleanup in onDestroy prevents ScrollTrigger leaks
6. Components ready for import into +page.svelte (Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-sections/04-01-SUMMARY.md`
</output>
