---
phase: 04-content-sections
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/routes/+page.svelte
autonomous: false

must_haves:
  truths:
    - "Home page displays Problems, Services, and Closing sections in correct order after hero"
    - "Section ordering matches Canva comp: Hero -> Problems -> Services -> Closing -> (Portfolio placeholder) -> (Contact placeholder)"
    - "Navigation links (#about, #services) scroll to correct content sections"
    - "Background shifts between sections create visual 'soft anchor' arrival cues"
    - "Content sections render correctly on both desktop and mobile viewports"
    - "Scroll-triggered animations fire when sections enter viewport"
  artifacts:
    - path: "src/routes/+page.svelte"
      provides: "Home page with Hero, ProblemsSolved, Services, ClosingStatement, and remaining placeholders"
      contains: "ProblemsSolved"
  key_links:
    - from: "src/routes/+page.svelte"
      to: "src/lib/components/content/ProblemsSolved.svelte"
      via: "import and render"
      pattern: "import ProblemsSolved"
    - from: "src/routes/+page.svelte"
      to: "src/lib/components/content/Services.svelte"
      via: "import and render"
      pattern: "import Services"
    - from: "src/routes/+page.svelte"
      to: "src/lib/components/content/ClosingStatement.svelte"
      via: "import and render"
      pattern: "import ClosingStatement"
    - from: "Navigation.svelte #about link"
      to: "ProblemsSolved.svelte id=about"
      via: "anchor href scroll"
      pattern: "id=\"about\""
    - from: "Navigation.svelte #services link"
      to: "Services.svelte id=services"
      via: "anchor href scroll"
      pattern: "id=\"services\""
---

<objective>
Integrate all Phase 4 content components into the home page, replacing placeholder sections with real content. Update section ordering to match Canva comp hierarchy. Verify the full page renders correctly with navigation, scroll animations, and responsive behavior.

Purpose: Wire the content layer into the live page so visitors experience the complete narrative flow from hero through problems, services, and closing statement.

Output: Updated `+page.svelte` with content sections, verified via dev server and visual checkpoint.
</objective>

<execution_context>
@/home/dom/.claude/get-shit-done/workflows/execute-plan.md
@/home/dom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-content-sections/04-01-SUMMARY.md
@.planning/phases/04-content-sections/04-CONTEXT.md
@src/routes/+page.svelte
@src/lib/components/navigation/Navigation.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate content components into home page</name>
  <files>src/routes/+page.svelte</files>
  <action>
Update `src/routes/+page.svelte` to:

1. **Add imports** for ProblemsSolved, Services, and ClosingStatement from `$lib/components/content/`
2. **Remove** the old Work, Services, and About placeholder sections (keep only the Contact placeholder)
3. **Remove** the unused SectionHeading and animate imports if no longer needed in this file (they're now inside the content components)

**New page structure (in order):**
```
<Hero />
<ProblemsSolved />
<Services />
<ClosingStatement />

<!-- Portfolio placeholder (Phase 5) -->
<section id="work" class="section">
  <Container>
    <p style="font-family: var(--font-sans); color: var(--color-text-muted); font-size: var(--text-sm);">
      Portfolio section — Phase 5
    </p>
  </Container>
</section>

<!-- Contact placeholder (Phase 7) -->
<section id="contact" class="section section--dark">
  <Container>
    <p style="font-family: var(--font-sans); color: var(--color-surface); opacity: 0.6; font-size: var(--text-sm);">
      Contact section — Phase 7
    </p>
  </Container>
</section>
```

4. **Simplify styles:** Remove all the placeholder CSS that was only used by the old sections (`.section-content`, `.section-text`, `.section-text--large`, `.section-text--light`, `.placeholder-grid`, `.placeholder-card`, `.services-list`, `.service-item`, `.service-title`, `.service-desc`). Keep only:
   - `.section` base (padding, background-color)
   - `.section--warm` modifier
   - `.section--dark` modifier
   - `.contact-cta` styles (still used by Contact placeholder)
   - Responsive media query for section padding

5. **Keep Container import** since the placeholders still use it. Remove SectionHeading and animate imports if no remaining usage in the file.

6. **Section ID preservation:** ProblemsSolved internally has `id="about"`, Services has `id="services"`, Portfolio placeholder keeps `id="work"`, Contact placeholder keeps `id="contact"`. All navigation scroll targets remain functional.

7. **Verify dev server:** Run `npm run dev` and check the page loads without errors in the console. Check that the dev server builds successfully.
  </action>
  <verify>
- `npm run build` completes without errors
- `grep -c 'ProblemsSolved\|Services\|ClosingStatement' src/routes/+page.svelte` returns 6 (3 imports + 3 component uses)
- `grep -c 'placeholder-grid\|service-item' src/routes/+page.svelte` returns 0 (old placeholder code removed)
- Page structure follows order: Hero, ProblemsSolved, Services, ClosingStatement, work placeholder, contact placeholder
  </verify>
  <done>
Home page renders Hero, then Problems Solved section, then Services section, then Closing Statement, followed by Portfolio and Contact placeholders. All old placeholder content removed. Navigation links (#about, #services, #work, #contact) point to correct section IDs.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete content sections (Problems Solved, Services, Closing Statement) integrated into the home page with:
- Editorial typography hierarchy (Playfair Display headings, Source Sans 3 body)
- Confrontational problem cards in 2-column grid
- 5 numbered service pillars (01-05) in vertical stack with thin dividers
- Weighted closing statement centered
- Background shifts between sections (cream -> warm cream -> cream)
- Scroll-triggered staggered reveal animations
- Responsive mobile layout (single column, simplified animations)
  </what-built>
  <how-to-verify>
1. Open http://localhost:5199 in browser (run `npm run dev` if not already running)
2. Scroll past hero — verify "Where art and systems flow together" heading appears with scroll animation
3. Verify intro paragraph and "Problems we help solve" label are visible
4. Verify all 6 problem statements are visible in a 2-column grid (desktop) or single column (mobile)
5. Continue scrolling — verify background shifts to warm cream for "What we build" section
6. Verify all 5 services (01-05) display with numbers, taglines, descriptions, and bullet lists
7. Verify thin dividers separate each service pillar
8. Continue scrolling — verify "Entry points are flexible. Outputs are deliberate." appears centered with visual weight
9. Test navigation: click "About" nav link — should scroll to Problems section. Click "Services" — should scroll to Services section.
10. Resize to mobile width (~375px) — verify single-column layout, readable type sizes, sections still fully visible
11. Check browser console for GSAP errors or warnings (should be clean)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
After integration:
1. `npm run build` succeeds without errors
2. All 4 navigation links (#work, #services, #about, #contact) reach correct sections
3. Page content order: Hero -> Problems -> Services -> Closing -> Portfolio (placeholder) -> Contact (placeholder)
4. No console errors or GSAP warnings
5. Responsive layout works on desktop and mobile
6. Scroll animations trigger on both desktop and mobile
7. All content matches CONTENT.md text exactly
</verification>

<success_criteria>
1. Visitor scrolling the page sees a complete narrative: hero -> problems -> services -> closing statement
2. Navigation links scroll to correct sections
3. Background shifts create distinct "chapter" feel between sections
4. Typography hierarchy is consistent with design system tokens
5. Mobile layout is readable and functional
6. No build errors or console warnings
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-sections/04-02-SUMMARY.md`
</output>
