---
phase: 02-navigation-layout
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/lib/components/layout/Footer.svelte
  - src/routes/+layout.svelte
  - src/routes/+page.svelte
autonomous: false

must_haves:
  truths:
    - "Footer displays Brisbane HQ and Los Angeles office addresses"
    - "Footer includes contact email ohhello@subfrac.com"
    - "Navigation is visible from any scroll position"
    - "Scroll progress indicator appears at top of page"
    - "Mobile menu works on narrow viewports"
  artifacts:
    - path: "src/lib/components/layout/Footer.svelte"
      provides: "Site footer with office addresses and contact info"
      min_lines: 50
    - path: "src/routes/+layout.svelte"
      provides: "Root layout with Navigation and Footer integrated"
      contains: "Navigation"
  key_links:
    - from: "src/routes/+layout.svelte"
      to: "src/lib/components/navigation/Navigation.svelte"
      via: "import and render"
      pattern: "import.*Navigation"
    - from: "src/routes/+layout.svelte"
      to: "src/lib/components/navigation/ScrollProgress.svelte"
      via: "import and render"
      pattern: "import.*ScrollProgress"
    - from: "src/routes/+layout.svelte"
      to: "src/lib/components/layout/Footer.svelte"
      via: "import and render"
      pattern: "import.*Footer"
---

<objective>
Create the footer component and integrate navigation system into the site layout.

Purpose: Complete the persistent UI structure that wraps all page content.
Output: Footer component and updated layout with navigation, progress, and footer.
</objective>

<execution_context>
@/home/dom/.claude/get-shit-done/workflows/execute-plan.md
@/home/dom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-navigation-layout/02-RESEARCH.md
@.planning/phases/02-navigation-layout/02-01-SUMMARY.md
@src/routes/+layout.svelte
@src/lib/styles/tokens.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Footer component</name>
  <files>src/lib/components/layout/Footer.svelte</files>
  <action>
Create the site footer with office addresses and contact information:

**Layout:**
- Dark background (ink/black from tokens)
- Generous padding (py-16 or similar)
- Use Container component with wide variant

**Content structure (two or three columns on desktop):**

Column 1 - Wordmark/Brand:
- "SUBFRACTURE" wordmark in cream
- "Culture Studio" tagline in smaller text

Column 2 - Offices:
- "Brisbane HQ" heading
- Address (to be filled with real address or placeholder)
- Phone/contact if available

- "Los Angeles" heading
- Address (to be filled with real address or placeholder)

Column 3 - Contact:
- Email: ohhello@subfrac.com (use mailto: link)
- Copyright line with current year

**Typography:**
- Use design tokens (text-sm for body, text-xs for labels)
- Cream text on dark background
- Terracotta for links/accents

**Responsive:**
- Stack columns on mobile
- Generous spacing between sections
  </action>
  <verify>npm run check passes. Footer renders with cream text on dark background.</verify>
  <done>Footer displays both office locations and contact email.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate navigation into layout</name>
  <files>src/routes/+layout.svelte</files>
  <action>
Update the root layout to include navigation components:

**Imports to add:**
- Navigation from $lib/components/navigation/Navigation.svelte
- ScrollProgress from $lib/components/navigation/ScrollProgress.svelte
- Footer from $lib/components/layout/Footer.svelte

**Structure:**
```svelte
<ScrollProgress />
<Navigation />

<main>
  {@render children()}
</main>

<Footer />
```

**Layout considerations:**
- Add padding-top to main to account for fixed navigation height (~64-80px)
- Ensure scroll progress is above navigation (z-index ordering)
- Keep existing GSAP initialization from Phase 1

**Do NOT remove:**
- Font imports (@fontsource)
- GSAP initialization in onMount
- Any existing CSS imports
  </action>
  <verify>npm run dev shows navigation at top, footer at bottom, content scrolls between.</verify>
  <done>Layout includes Navigation, ScrollProgress, and Footer wrapping page content.</done>
</task>

<task type="auto">
  <name>Task 3: Add page sections for navigation testing</name>
  <files>src/routes/+page.svelte</files>
  <action>
Update the demo page to include sections that navigation links target:

**Add section IDs:**
- id="work" to a section (can be placeholder)
- id="services" to a section
- id="about" to a section
- id="contact" to a section

**Use existing SectionHeading component:**
Each section should have a SectionHeading with appropriate number:
- 01 Work
- 02 Services
- 03 About
- 04 Contact

**Content:**
Keep existing design system demos OR replace with placeholder sections.
Main goal is having anchor targets for navigation links.

**Scroll behavior:**
Sections should have enough height to test scroll behavior and progress indicator.
  </action>
  <verify>Clicking nav links scrolls to corresponding sections.</verify>
  <done>Page has section anchors matching navigation links.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete navigation system with:
- Sticky navigation that auto-hides on scroll down, shows on scroll up
- Mobile menu with hamburger toggle (44x44px touch target)
- Scroll progress indicator at top of page
- Footer with Brisbane HQ and Los Angeles addresses
- Section anchors for navigation links
  </what-built>
  <how-to-verify>
1. Open http://localhost:5173 in browser
2. **Desktop navigation:**
   - See navigation bar at top with logo and links
   - Scroll down - navigation should hide
   - Scroll up - navigation should reappear
   - Click nav links - should scroll to sections

3. **Scroll progress:**
   - See terracotta progress bar at very top
   - Scroll down - bar should fill from left to right
   - Reach bottom - bar should be 100% filled

4. **Mobile menu (resize to <768px or use DevTools):**
   - See hamburger icon on right
   - Tap/click hamburger - menu should slide in
   - Links should be visible and tappable
   - Tap a link - menu closes, page scrolls
   - Press Escape - menu closes

5. **Footer:**
   - Scroll to bottom of page
   - See dark footer with cream text
   - Brisbane HQ and Los Angeles addresses visible
   - Email link visible (ohhello@subfrac.com)

6. **Accessibility (DevTools):**
   - Mobile toggle has aria-expanded="true/false"
   - Mobile toggle has aria-controls="mobile-menu"
   - Progress bar has role="progressbar" with aria-valuenow
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues to fix.</resume-signal>
</task>

</tasks>

<verification>
Run these commands after implementation:

```bash
# Type checking
npm run check

# Dev server
npm run dev

# Verify component structure
ls -la src/lib/components/navigation/
ls -la src/lib/components/layout/
```

Manual verification checklist:
- [ ] Navigation visible at top
- [ ] Navigation hides on scroll down
- [ ] Navigation shows on scroll up
- [ ] Progress bar fills with scroll
- [ ] Mobile menu opens/closes
- [ ] Mobile toggle has correct ARIA
- [ ] Footer visible at bottom
- [ ] Footer has both addresses
- [ ] Nav links scroll to sections
</verification>

<success_criteria>
1. Footer component exists with Brisbane and LA addresses
2. Layout integrates Navigation, ScrollProgress, and Footer
3. Page has section anchors matching navigation links
4. Navigation auto-hide works correctly
5. Mobile menu opens/closes with accessibility attributes
6. Scroll progress indicator fills 0-100% with page scroll
7. User verification passes all checks
</success_criteria>

<output>
After completion, create `.planning/phases/02-navigation-layout/02-02-SUMMARY.md` following the summary template.
</output>
