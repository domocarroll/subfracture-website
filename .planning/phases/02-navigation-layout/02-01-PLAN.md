---
phase: 02-navigation-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/components/navigation/Navigation.svelte
  - src/lib/components/navigation/NavLink.svelte
  - src/lib/components/navigation/MobileMenu.svelte
  - src/lib/components/navigation/ScrollProgress.svelte
autonomous: true

must_haves:
  truths:
    - "Navigation bar is visible when scrolling up, hidden when scrolling down"
    - "Mobile menu toggle button has 44x44px touch target"
    - "Mobile menu announces state to screen readers via aria-expanded"
    - "Scroll progress bar fills based on page scroll position"
    - "Reduced motion users see instant state changes"
  artifacts:
    - path: "src/lib/components/navigation/Navigation.svelte"
      provides: "Sticky header with auto-hide behavior and mobile toggle"
      min_lines: 80
    - path: "src/lib/components/navigation/NavLink.svelte"
      provides: "Reusable nav link with active state detection"
      min_lines: 20
    - path: "src/lib/components/navigation/MobileMenu.svelte"
      provides: "Slide-out mobile menu with Disclosure pattern"
      min_lines: 40
    - path: "src/lib/components/navigation/ScrollProgress.svelte"
      provides: "Scroll progress indicator bar"
      min_lines: 40
  key_links:
    - from: "src/lib/components/navigation/Navigation.svelte"
      to: "GSAP ScrollTrigger"
      via: "direction detection for auto-hide"
      pattern: "ScrollTrigger\\.create.*direction"
    - from: "src/lib/components/navigation/ScrollProgress.svelte"
      to: "GSAP ScrollTrigger"
      via: "progress binding"
      pattern: "self\\.progress"
    - from: "src/lib/components/navigation/Navigation.svelte"
      to: "$lib/utils/motion"
      via: "reduced motion check"
      pattern: "prefersReducedMotion"
---

<objective>
Create the navigation component system with sticky header, mobile menu, and scroll progress indicator.

Purpose: Establish persistent UI orientation that helps visitors navigate the single-page layout.
Output: Four navigation components ready for integration into the site layout.
</objective>

<execution_context>
@/home/dom/.claude/get-shit-done/workflows/execute-plan.md
@/home/dom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-navigation-layout/02-RESEARCH.md
@.planning/phases/01-foundation-design-system/01-02-SUMMARY.md
@src/lib/utils/gsap.ts
@src/lib/utils/motion.ts
@src/lib/styles/tokens.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NavLink component</name>
  <files>src/lib/components/navigation/NavLink.svelte</files>
  <action>
Create a reusable navigation link component with the following features:

**Props:**
- `href: string` - Link destination (section anchor like #work, #about)
- `children: Snippet` - Link text content

**Styling:**
- Use Source Sans 3 font (font-ui utility from tokens)
- Terracotta underline on hover (via CSS transition)
- Uppercase tracking-wide for editorial feel
- text-sm size

**Active state:**
- Detect current hash/section using page store or scroll position
- Apply terracotta color when active

**Accessibility:**
- Use semantic `<a>` element
- Include aria-current="page" when active
  </action>
  <verify>Component file exists at path. Uses proper Svelte 5 syntax ($props, Snippet).</verify>
  <done>NavLink component renders links with hover states and active detection.</done>
</task>

<task type="auto">
  <name>Task 2: Create ScrollProgress component</name>
  <files>src/lib/components/navigation/ScrollProgress.svelte</files>
  <action>
Create the scroll progress indicator bar with the following features:

**Visual:**
- Fixed to top of viewport (top-0 left-0)
- Height 2px (or 0.5 for subtlety)
- Terracotta background (bg-primary)
- z-index above navigation (z-[60])
- Width expands from 0% to 100% based on scroll

**Scroll tracking:**
- Use GSAP ScrollTrigger with onUpdate callback
- Bind to document.documentElement for full page height
- Update progress state (0 to 1) from self.progress

**SSR safety:**
- Import browser from $app/environment
- Dynamic import GSAP in onMount
- Store ScrollTrigger reference for cleanup in onDestroy

**Accessibility:**
- role="progressbar"
- aria-valuenow (0-100)
- aria-valuemin={0}
- aria-valuemax={100}
- aria-label="Reading progress"
  </action>
  <verify>npm run check passes. Component uses initGSAP() from $lib/utils/gsap.</verify>
  <done>Progress bar fills based on scroll position with proper ARIA attributes.</done>
</task>

<task type="auto">
  <name>Task 3: Create MobileMenu component</name>
  <files>src/lib/components/navigation/MobileMenu.svelte</files>
  <action>
Create the mobile menu using the Disclosure pattern:

**Props:**
- `isOpen: boolean` - Controlled open state (bindable)
- `onClose: () => void` - Callback when menu should close

**Structure:**
- Full-screen overlay with cream background
- Stack navigation links vertically with generous spacing
- Close button in top-right corner

**Animation:**
- Use Svelte slide transition from svelte/transition
- Check prefersReducedMotion() to set duration to 0 if needed
- Slide in from top or right

**Accessibility (Disclosure pattern):**
- id="mobile-menu" to match aria-controls on toggle button
- Focus trap not required for this pattern
- Links should be focusable when menu is open

**Escape key:**
- Close menu on Escape key press
- Call onClose callback

**Links:**
- Use NavLink component for each link
- Close menu after clicking a link
  </action>
  <verify>Component uses slide transition with reduced motion check.</verify>
  <done>Mobile menu slides in/out with proper accessibility and keyboard support.</done>
</task>

<task type="auto">
  <name>Task 4: Create Navigation component</name>
  <files>src/lib/components/navigation/Navigation.svelte</files>
  <action>
Create the main navigation component combining all pieces:

**Layout:**
- Fixed header (position: fixed, top-0, left-0, right-0)
- Cream background with subtle shadow when scrolled
- z-index z-50 (below scroll progress)
- Horizontal padding matching Container component

**Desktop navigation (hidden md:flex):**
- Logo/wordmark on left (text, link to #top or /)
- NavLink components on right for: Work, Services, About, Contact

**Mobile toggle (md:hidden):**
- Hamburger icon button on right
- 44x44px touch target (min-h-11 min-w-11)
- aria-expanded binding to isMenuOpen state
- aria-controls="mobile-menu"
- aria-label that changes based on state

**Auto-hide behavior:**
- Use GSAP ScrollTrigger to detect scroll direction
- direction === 1 (down) -> hide nav (translate-y-[-100%])
- direction === -1 (up) -> show nav (translate-y-0)
- Always show when scrollY < 80px
- CSS transition for smooth show/hide

**State management:**
- isMenuOpen state for mobile menu
- isNavVisible state for auto-hide
- scrollY bound via <svelte:window>

**Escape key handling:**
- Listen on window keydown
- Close mobile menu on Escape

**Cleanup:**
- Store ScrollTrigger reference
- Kill in onDestroy
  </action>
  <verify>npm run check passes. Navigation uses GSAP ScrollTrigger with proper cleanup.</verify>
  <done>Navigation shows/hides on scroll direction with working mobile toggle.</done>
</task>

</tasks>

<verification>
Run these commands to verify all navigation components:

```bash
# Type checking
npm run check

# Visual verification (dev server should be running)
# Check http://localhost:5173 for navigation behavior

# Verify file structure
ls -la src/lib/components/navigation/
```

Expected structure:
```
src/lib/components/navigation/
├── Navigation.svelte
├── NavLink.svelte
├── MobileMenu.svelte
└── ScrollProgress.svelte
```
</verification>

<success_criteria>
1. All four navigation components exist and pass type checking
2. Navigation uses GSAP ScrollTrigger for direction detection
3. ScrollProgress uses GSAP ScrollTrigger for progress binding
4. MobileMenu uses Svelte transition with reduced motion check
5. Mobile toggle has 44x44px touch target with proper ARIA attributes
6. All components use SSR-safe patterns (browser check, dynamic imports)
</success_criteria>

<output>
After completion, create `.planning/phases/02-navigation-layout/02-01-SUMMARY.md` following the summary template.
</output>
