---
phase: 01-foundation-design-system
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - src/lib/components/ui/SectionHeading.svelte
  - src/lib/components/ui/Container.svelte
  - src/routes/+page.svelte
  - src/routes/+layout.svelte
autonomous: false

must_haves:
  truths:
    - "Terracotta headings appear on cream background"
    - "Typography uses Playfair Display at golden ratio scale"
    - "Section numbers (01, 02...) display with consistent styling"
    - "Two-column layout collapses to single-column on mobile"
    - "Reveal animations work with fade + rise effect"
    - "No console errors or hydration warnings"
  artifacts:
    - path: "src/lib/components/ui/SectionHeading.svelte"
      provides: "Section heading with number component"
      contains: "section-number"
    - path: "src/lib/components/ui/Container.svelte"
      provides: "Responsive container with grid options"
      contains: "content-max"
    - path: "src/routes/+page.svelte"
      provides: "Demo page showing design system"
      contains: "SectionHeading"
  key_links:
    - from: "src/lib/components/ui/SectionHeading.svelte"
      to: "src/lib/styles/tokens.css"
      via: "CSS custom properties"
      pattern: "var\\(--color-primary\\)"
    - from: "src/routes/+page.svelte"
      to: "src/lib/actions/animate.ts"
      via: "use:animate"
      pattern: "use:animate"
    - from: "src/routes/+page.svelte"
      to: "src/lib/components/ui/SectionHeading.svelte"
      via: "component import"
      pattern: "import.*SectionHeading"
---

<objective>
Create typography and grid components, then build a demo page that validates the entire design system.

Purpose: Demonstrate that all Phase 1 foundations work together - tokens, fonts, grid, animations. This provides a visual checkpoint for the user and validates success criteria.

Output:
- SectionHeading component with numbered labels
- Container component with responsive grid
- Demo page showing typography scale, colors, grid, and animations
</objective>

<execution_context>
@/home/dom/.claude/get-shit-done/workflows/execute-plan.md
@/home/dom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-design-system/01-CONTEXT.md
@.planning/phases/01-foundation-design-system/01-RESEARCH.md
@.planning/phases/01-foundation-design-system/01-01-SUMMARY.md
@.planning/phases/01-foundation-design-system/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create foundational UI components</name>
  <files>src/lib/components/ui/SectionHeading.svelte, src/lib/components/ui/Container.svelte</files>
  <action>
**Create src/lib/components/ui/SectionHeading.svelte:**

A section heading component with numbered label following RESEARCH.md Pattern 4.

Props:
- `number: string` - Section number ("01", "02", etc.)
- `title: string` - Heading text
- `level?: 'h1' | 'h2' | 'h3'` - Semantic heading level (default 'h2')

Styling:
- Section number in terracotta (--color-primary), using --font-sans
- Number is small (--text-xs), slightly raised like superscript
- Title in --font-serif, --color-text
- Flexbox layout with baseline alignment
- Gap between number and title: 0.75rem

**Create src/lib/components/ui/Container.svelte:**

A responsive container for content sections.

Props:
- `variant?: 'narrow' | 'wide'` - Width constraint (default 'narrow')
- `grid?: boolean` - Enable two-column grid (default false)

Styling:
- narrow: max-width of --content-max (900px)
- wide: max-width of --content-wide (1200px)
- Centered with mx-auto
- Horizontal padding: responsive (1rem mobile, 1.5rem tablet, 2rem desktop)
- When grid=true: apply golden ratio columns (1.618fr 1fr) above md breakpoint

Use CSS custom properties from tokens.css for all values.
  </action>
  <verify>
1. Both files exist in src/lib/components/ui/
2. SectionHeading uses var(--color-primary) for number
3. Container uses var(--content-max) for width
4. TypeScript types are correct (no errors in svelte-check)
  </verify>
  <done>
SectionHeading and Container components created with:
- Proper use of design tokens
- Responsive behavior
- TypeScript typing
- Svelte 5 syntax ($props())
  </done>
</task>

<task type="auto">
  <name>Task 2: Build design system demo page</name>
  <files>src/routes/+page.svelte</files>
  <action>
Replace the existing +page.svelte with a comprehensive design system demo.

**Page structure:**

1. **Hero section:**
   - Large display heading (--text-5xl or --text-6xl) saying "Design System"
   - Subheading in italic Playfair
   - Background: --color-surface

2. **Typography scale section:**
   - SectionHeading with number="01" title="Typography"
   - Display all type sizes from --text-6xl down to --text-xs
   - Show both Playfair Display (serif) and Source Sans 3 (sans)
   - Use Container variant="narrow"

3. **Color palette section:**
   - SectionHeading with number="02" title="Color Palette"
   - Color swatches for all semantic colors (surface, primary, text, etc.)
   - Display hex values
   - Use two-column grid (Container with grid=true)

4. **Grid demonstration section:**
   - SectionHeading with number="03" title="Grid System"
   - Two-column golden ratio layout example
   - Content showing major/minor column proportions
   - Demonstrates collapse on mobile

5. **Animation showcase section:**
   - SectionHeading with number="04" title="Motion"
   - Several elements with use:animate directive
   - Demonstrate fade + rise reveal animation
   - Show ScrollTrigger activation (elements animate on scroll into view)

**Animation examples to include:**
```svelte
<div use:animate={{
  type: 'from',
  opacity: 0,
  y: 30,
  duration: 0.6,
  scrollTrigger: { start: 'top 85%' }
}}>
  This reveals on scroll
</div>
```

**Spacing:**
- Use --spacing-section (10rem) between major sections
- Generous vertical rhythm throughout

**Critical:** Import animate action from '$lib/actions/animate' and use it on demo elements.
  </action>
  <verify>
1. Run `npm run dev` and navigate to http://localhost:5173
2. Page displays without console errors
3. Typography section shows all sizes
4. Color palette section shows all colors
5. Grid collapses to single column on mobile (resize browser)
6. Animation section elements fade in on scroll
7. No hydration errors in terminal
  </verify>
  <done>
Demo page displays all design system elements:
- Typography scale visible (Playfair Display and Source Sans 3)
- Color palette with terracotta, cream, bone, botanical, burgundy
- Grid with golden ratio columns that collapse on mobile
- Animations triggering on scroll
- No errors or warnings
  </done>
</task>

<task type="auto">
  <name>Task 3: Update layout with GSAP initialization</name>
  <files>src/routes/+layout.svelte</files>
  <action>
Update +layout.svelte to initialize GSAP on mount.

**Changes:**
1. Keep existing font imports (from Plan 01)
2. Add GSAP initialization:
   - Import `onMount` from 'svelte'
   - Import `initGSAP` from '$lib/utils/gsap'
   - Call initGSAP() in onMount

**Final layout structure:**
```svelte
<script lang="ts">
  import '@fontsource-variable/playfair-display';
  import '@fontsource/source-sans-3/400.css';
  import '@fontsource/source-sans-3/500.css';
  import '@fontsource/source-sans-3/600.css';
  import '../app.css';

  import { onMount } from 'svelte';
  import { initGSAP } from '$lib/utils/gsap';

  let { children } = $props();

  onMount(() => {
    initGSAP();
  });
</script>

{@render children()}
```

This ensures GSAP is registered before any components try to use it.
  </action>
  <verify>
1. Layout contains onMount with initGSAP call
2. GSAP animations work in demo page
3. No console errors about missing GSAP or ScrollTrigger
  </verify>
  <done>
Layout initializes GSAP on mount, enabling animations throughout the site.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 1 design system including:
- Design tokens (colors, typography, spacing, easing)
- Self-hosted fonts (Playfair Display, Source Sans 3)
- GSAP utilities with SSR safety and cleanup
- SectionHeading and Container components
- Demo page showing the entire design system
  </what-built>
  <how-to-verify>
1. Run `npm run dev` and open http://localhost:5173
2. **Typography check:**
   - Verify headings use Playfair Display (elegant serif)
   - Verify UI elements use Source Sans 3 (clean sans-serif)
   - Check the typography scale section shows progression from small to large
3. **Color check:**
   - Background should be cream/parchment (not pure white)
   - Section numbers should be terracotta (#c55a3d)
   - Verify NO mint or magenta colors appear anywhere
4. **Grid check:**
   - On desktop (1024px+): two-column grid should show golden ratio (wider left, narrower right)
   - On mobile (<768px): should collapse to single column
5. **Animation check:**
   - Scroll down to "Motion" section
   - Elements should fade in and rise slightly as they enter viewport
6. **Console check:**
   - Open browser DevTools > Console
   - Should see no errors about hydration, GSAP, or fonts
   - Network tab should show no fonts.googleapis.com requests
  </how-to-verify>
  <resume-signal>
Type "approved" if everything looks correct, or describe any issues you see.
  </resume-signal>
</task>

</tasks>

<verification>
1. `npm run dev` starts without errors
2. Demo page loads and displays all sections
3. Typography uses correct fonts (Playfair Display, Source Sans 3)
4. Colors match palette (terracotta, cream, bone - no mint/magenta)
5. Grid collapses responsively at md breakpoint
6. Scroll-triggered animations work
7. No console errors or hydration warnings
8. Network tab shows no Google Fonts requests
</verification>

<success_criteria>
1. Terracotta headings on cream background (FOUND-01, FOUND-02)
2. Typography scale displays golden ratio progression (FOUND-01)
3. Section numbers appear with consistent styling (FOUND-01)
4. Two-column grid collapses to single-column on mobile (FOUND-03)
5. Animations work with fade + rise effect (FOUND-05)
6. No SSR/hydration errors (FOUND-05)
7. All design tokens accessible via CSS custom properties (FOUND-04)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-system/01-03-SUMMARY.md`
</output>
